export type Product = {
  id: string;
  name: string;
  description: string;
  price: number;
  stock: number;
  category: string;
  created_at: string; // ISO date string
  updated_at: string; // ISO date string
};
