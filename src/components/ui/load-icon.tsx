import { Loader, Loader2 } from "lucide-react";
import React from "react";

const LoadIcon = () => {
  return <Loader className="mx-auto my-auto h-4 w-4 animate-spin"></Loader>;
};

export default LoadIcon;
